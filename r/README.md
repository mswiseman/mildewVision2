## Reproducibility
As long as the repository is cloned, it's file structure is maintained, and the proper `r` libraries are install using `install.packages("")`, then all the .Rmd code should be reproducible. 
